const HomeReducer = (state, action) => {
    switch (action.type) {
        case 'Check_Is_Logged_In':
            // return {
            //     ...state,
            //     isLoggedIn: 
            // }
    
        default:
            return state
    }
}

export default HomeReducer